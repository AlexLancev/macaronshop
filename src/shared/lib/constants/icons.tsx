import { LucideProps } from "lucide-react";

/**
 * Глобальные иконки для использования по всему приложению
 */
export const globalIcons = {
	// Добавьте здесь общие иконки, которые используются в нескольких местах
} as const;

export type GlobalIconKeys = keyof typeof globalIcons;
